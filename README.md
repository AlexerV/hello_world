# hello_world

- Python : `python {name}`
- C : `gcc {name}` + `a.out`
- C++ : `g++ {name}` + `a.out`
- C# : `?`
- JS : `node {name}`
- Java : `java {name}`
- Go : `go run {name}`
- Rust ; `rustc {name}` + `./{name}`
